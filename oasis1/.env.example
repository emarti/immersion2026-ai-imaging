# Copy this file to .env and set the path to your extracted OASIS-1 dataset root.
# .env is gitignored so each machine keeps its own path. See readme.md
# ("Configure data paths") for details.
#
# The directory should contain one folder per DVD: disc1/ disc2/ ... disc12/
DATA_RAW_PATH=/path/to/oasis
